const Projects = () => {
  return (
    <div style={{ color: "white" }}>
      Projects cards and links cards will contain images and description of the
      projects
    </div>
  );
};

export default Projects;
